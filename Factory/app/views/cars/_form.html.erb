<%= form_with(model: car, local: true) do |form| %>
  <% if car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(car.errors.count, "error") %> prohibited this car from being saved:</h2>

      <ul>
      <% car.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Make %>
    <%= form.text_field :Make %>
  </div>

  <div class="field">
    <%= form.label :Model %>
    <%= form.text_field :Model %>
  </div>

  <div class="field">
    <%= form.label :Vin %>
    <%= form.text_field :Vin %>
  </div>

  <div class="field">
    <%= form.label :Country %>
    <%= form.text_field :Country %>
  </div>

  <div class="field">
    <%= form.label :cars_parts %><br>
    <%= form.collection_select :make_id, Make.all, :id, :name %><!-- TODO: This still doesn't create a dropdown -->
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
